import {UseFormRegister} from "react-hook-form";
import {DocumentInput} from "../Network/Document_api.ts";
import {Box} from "@mui/material";

export function ProfilePicture({register} :{ register : UseFormRegister<DocumentInput>}){


    return(
        <Box>

        </Box>
    )
}